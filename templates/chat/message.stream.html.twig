
  <turbo-stream action="append" target="messages">
    <template>
   
      {% if message.fromId == chat.users[1].id %}
      {# {% if app.user.id == fromUserId %} #}
        <div class="message-wrapper reverse turbo-stream">
        {# <img class="message-pp" src="{{ asset('uploads/profiles/' ~ app.user.picture) }}" alt="profile-pic"> #}
        <img class="message-pp" src="{{ asset('uploads/profiles/' ~ chat.users[1].picture) }}" alt="profile-pic">
          {# <img class="message-pp" src="https://images.unsplash.com/photo-1438761681033-6461ffad8d80?ixid=MXwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHw%3D&ixlib=rb-1.2.1&auto=format&fit=crop&w=2550&q=80" alt="profile-pic"> #}
          {# <span class="message-pp">{{ chat.users[1].firstname}}</span> #}
      {% else %}
          <div class="message-wrapper turbo-stream">
        <img class="message-pp" src="{{ asset('uploads/profiles/' ~ chat.users[0].picture) }}" alt="profile-pic">
          {# <span class="message-pp">{{ chat.users[0].firstname}}</span> #}
      {% endif %}
          <div class="message-box-wrapper">
          <div class="message-box">
          {% if message.attachments is defined %}
          {% for attach in message.attachments %}
              <a href="{{ asset('/uploads/message/' ~ attach.url) }}" target="blank" >
              <embed src="{{ asset('/uploads/message/' ~ attach.url) }}" alt="" width="150">
              </a>
                <br>
          {% endfor %}
          
          {% endif %}
         
          {{message.content}}
          </div>
            <span>{{message.createdAt|date('d M H:i')}}</span>
          </div>
        </div>
        
    </template>
</turbo-stream >

 {# <turbo-stream action="replace" target="empty-message_{{app.user.id}}">
            <template>
               <script id='empty-message_{{app.user.id}}' type="module">
                const form = document.getElementById('message_content')
                form.value = ''
              </script>
            </template>
</turbo-stream> #}


 {# <turbo-stream action="replace" target="scroll_message">
            <template>
               <script id='scroll_message' type="module">
                const chatDiv = document.querySelector('.overflow-auto');
                chatDiv.scrollTop = chatDiv.scrollHeight; 
              </script>
            </template>
</turbo-stream> #}
